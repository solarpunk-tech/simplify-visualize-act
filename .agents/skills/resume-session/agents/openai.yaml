interface:
  display_name: "Resume Session"
  short_description: "Reconstruct project state from the handoff and current git status"
  default_prompt: "Resume from handoff, report completion state and blockers first, verify branch/git status, then continue from the next pending action."

policy:
  allow_implicit_invocation: false
